yaml
name: ship_trajectory_prediction

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      config_path: {type: str, default: configs/config.yaml}
    command: >
      python src/main.py
      --config_path {config_path}
